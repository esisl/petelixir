defmodule PetelixirWeb.TelemetryJSON do
  # Этот модуль можно оставить пустым или использовать для сложных структур,
  # но для простого ответа мы уже сформировали map в контроллере.
  # Phoenix 1.7 позволяет возвращать map напрямую, если нет строгой схемы.
end